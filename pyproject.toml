[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "stream-analysis"
version = "0.1.0"
description = "H.264/H.265 bitstream analysis tool"
requires-python = ">=3.10"

[project.scripts]
stream-analysis = "stream_analysis.cli:main"
